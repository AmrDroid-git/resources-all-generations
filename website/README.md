# Student Resources Static Website

A static, multilingual website for collecting student resources from JSON files.

## What this version does

- French, light mode, and `classic` style are the defaults.
- The selected language/theme/style are saved in browser `localStorage`.
- Style presets are scalable: `normal`, `classic`, `bold`, `hacking`, `modern`, `academic`, `minimal`, `glass`.
- App/config files stay in `/data`.
- Resource/link JSON files stay in `/data/link/`.
- The website does **not** require you to manually edit an index file.
- A build script automatically generates `data/link/_files.json` from the real files in `data/link/`.
- University/category names are generated from the JSON filename only.
  - `iseah.json` becomes `ISEAH`.
  - `prepa.json` becomes `PREPA`.
  - `my-university.json` becomes `MY UNIVERSITY`.
- Counts and resource types are calculated from the real JSON files on page load.
- TXT download is generated directly in the browser.

## Project structure

```txt
.
├── index.html
├── home/index.html
├── resources/index.html
├── download/index.html
├── collaborate/index.html
├── about/index.html
├── 404.html
├── assets/
│   ├── css/styles.css
│   └── js/app.js
├── data/
│   ├── config.json
│   ├── i18n.json
│   ├── about.json
│   └── link/
│       ├── _files.json          # generated automatically, do not edit manually
│       ├── ensi.json
│       ├── prepa.json
│       ├── bac.json
│       └── ...other university/resource JSON files
├── scripts/
│   └── generate-resource-index.js
├── netlify.toml
├── vercel.json
├── package.json
└── README.md
```

## Why `_files.json` exists

Browsers cannot list files inside a folder on Netlify. That is why direct folder discovery fails on production.

So this project uses a generated file list:

```txt
data/link/_files.json
```

You do **not** edit it manually. It is generated by:

```bash
node scripts/generate-resource-index.js
```

Netlify runs this automatically because `netlify.toml` contains:

```toml
[build]
  publish = "."
  command = "node scripts/generate-resource-index.js"
```

This gives the easy workflow you wanted:

1. Add a new JSON file to `data/link/`.
2. Push/deploy.
3. Netlify regenerates `_files.json` automatically.
4. The website shows the new university/resource category.

No manual JS edit. No manual index edit.

## Local testing

Run:

```bash
node scripts/generate-resource-index.js
python -m http.server 8000
```

Then open:

```txt
http://localhost:8000/home/
```

## Config defaults

Edit `data/config.json`:

```json
{
  "last_modification_date": "18/06/2026",
  "resources_directory": "/data/link/",
  "resources_index_file": "/data/link/_files.json",
  "share_resources_link": "https://docs.google.com/forms/...",
  "defaults": {
    "language": "fr",
    "theme": "light",
    "style": "classic",
    "default_university": "ensi"
  }
}
```

## How to add a new university/resource category

Add one JSON file directly inside `data/link/`:

```txt
data/link/new-university.json
```

Use this format:

```json
[
  {
    "link": "https://drive.google.com/...",
    "name": "Resource name",
    "owner": "community",
    "description": "Useful description",
    "type": "folder"
  }
]
```

Then run the build script locally, or let Netlify run it during deployment:

```bash
node scripts/generate-resource-index.js
```

The displayed name comes from the filename:

```txt
new-university.json → NEW UNIVERSITY
iseah.json → ISEAH
```

## How to rename a university/category

Rename the file.

Example:

```txt
data/link/iseah.json → data/link/iseah-mahdia.json
```

Then regenerate:

```bash
node scripts/generate-resource-index.js
```

The label changes automatically:

```txt
ISEAH → ISEAH MAHDIA
```

## How to add a new style preset

1. Add it to `data/config.json` under `appearance.styles`.
2. Add matching CSS variables in `assets/css/styles.css`.

## How translations work

Interface text is in:

```txt
data/i18n.json
```

The resource JSON content itself is shown as provided.
